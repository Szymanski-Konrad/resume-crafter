import 'package:flutter/material.dart';
import 'package:resume_crafter/core/style/dimensions.dart';

class Gap {
  /// 1.0
  static const xxxs = SizedBox.square(dimension: AppDimens.xxxs);

  /// 2.0
  static const xxs = SizedBox.square(dimension: AppDimens.xxs);

  /// 4.0
  static const xs = SizedBox.square(dimension: AppDimens.xs);

  /// 8.0
  static const s = SizedBox.square(dimension: AppDimens.s);

  /// 16.0
  static const m = SizedBox.square(dimension: AppDimens.m);

  /// 24.0
  static const l = SizedBox.square(dimension: AppDimens.l);

  /// 32.0
  static const xl = SizedBox.square(dimension: AppDimens.xl);

  /// 40.0
  static const xxl = SizedBox.square(dimension: AppDimens.xxl);
}
