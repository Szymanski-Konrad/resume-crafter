import 'package:flutter/material.dart';
import 'package:resume_crafter/core/style/dimensions.dart';

class AppPaddings {
  static const pagePadding = EdgeInsets.all(AppDimens.m);

  static const xxxsAllPadding = EdgeInsets.all(AppDimens.xxxs);
  static const xxsAllPadding = EdgeInsets.all(AppDimens.xxs);
  static const xsAllPadding = EdgeInsets.all(AppDimens.xs);
  static const sAllPadding = EdgeInsets.all(AppDimens.s);
  static const mAllPadding = EdgeInsets.all(AppDimens.m);
  static const lAllPadding = EdgeInsets.all(AppDimens.l);
  static const xlAllPadding = EdgeInsets.all(AppDimens.xl);
  static const xxlAllPadding = EdgeInsets.all(AppDimens.xxl);

  static const xxxsVerticalPadding =
      EdgeInsets.symmetric(vertical: AppDimens.xxxs);
  static const xxsVerticalPadding =
      EdgeInsets.symmetric(vertical: AppDimens.xxs);
  static const xsVerticalPadding = EdgeInsets.symmetric(vertical: AppDimens.xs);
  static const sVerticalPadding = EdgeInsets.symmetric(vertical: AppDimens.s);
  static const mVerticalPadding = EdgeInsets.symmetric(vertical: AppDimens.m);
  static const lVerticalPadding = EdgeInsets.symmetric(vertical: AppDimens.l);
  static const xlVerticalPadding = EdgeInsets.symmetric(vertical: AppDimens.xl);
  static const xxlVerticalPadding =
      EdgeInsets.symmetric(vertical: AppDimens.xxl);

  static const xxxsHorizontalPadding =
      EdgeInsets.symmetric(horizontal: AppDimens.xxxs);
  static const xxsHorizontalPadding =
      EdgeInsets.symmetric(horizontal: AppDimens.xxs);
  static const xsHorizontalPadding =
      EdgeInsets.symmetric(horizontal: AppDimens.xs);
  static const sHorizontalPadding =
      EdgeInsets.symmetric(horizontal: AppDimens.s);
  static const mHorizontalPadding =
      EdgeInsets.symmetric(horizontal: AppDimens.m);
  static const lHorizontalPadding =
      EdgeInsets.symmetric(horizontal: AppDimens.l);
  static const xlHorizontalPadding =
      EdgeInsets.symmetric(horizontal: AppDimens.xl);
  static const xxlHorizontalPadding =
      EdgeInsets.symmetric(horizontal: AppDimens.xxl);
}
